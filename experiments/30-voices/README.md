# 30 voices

How is the voice controlled at synthesis? 007: zero-shot voice transfer from a reference clip (reference voices in its `config.json`, used by the panel). 013: the voice as an editable style tensor (save, load, interpolate) without the reference encoder; voicepacks written by `../voice.py` into `../voices/*.pt`.
